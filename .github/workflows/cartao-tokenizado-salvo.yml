name: Cartão salvo tokenizado - DESATIVADO

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  desativado:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Fluxo tokenizado desativado após regressão no checkout."
