name: Unificar entrega com central apos nova compra

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/unificar-entrega-com-central-apos-compra-2026-08-14.yml"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  corrigir:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Baixar codigo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Configurar Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm

      - name: Instalar dependencias
        run: npm ci --ignore-scripts

      - name: Mostrar todos os projetos da conta apos nova compra
        run: node scripts/unificar-entrega-com-central-apos-compra-2026-08-14.cjs

      - name: Validar pagina alterada
        run: npx eslint "src/pages/ENTREGA PROJETOS PRONTOS.hr1cn.js"

      - name: Conferir alteracao
        run: |
          git diff --check
          git diff -- "src/pages/ENTREGA PROJETOS PRONTOS.hr1cn.js"

      - name: Gravar no main
        run: |
          git config user.name "PELEGO BOX Automacao"
          git config user.email "pelegobox82@gmail.com"
          git add "src/pages/ENTREGA PROJETOS PRONTOS.hr1cn.js"
          if ! git diff --cached --quiet; then
            git commit -m "Exibir todos os projetos apos nova compra"
            git push origin HEAD:main
          fi
