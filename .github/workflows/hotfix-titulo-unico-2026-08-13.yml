name: Hotfix Titulo Unico Projetos Prontos (INATIVO)

on:
  workflow_dispatch:

jobs:
  inativo:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hotfix já aplicado ao código-fonte. Workflow mantido apenas como histórico e não altera mais o projeto."
