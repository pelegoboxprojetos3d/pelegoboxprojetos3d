name: Somar 3 etapas no botão total do checkout

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  aviso:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Workflow antigo desativado. O ajuste final está em fixar-total-ranking-2026-08-22.yml.'
