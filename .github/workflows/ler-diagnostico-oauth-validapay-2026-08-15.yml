name: Ler diagnóstico OAuth ValidaPay

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/ler-diagnostico-oauth-validapay-2026-08-15.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  ler:
    runs-on: ubuntu-latest
    steps:
      - name: Comparar rotas HTTP functions
        run: |
          set -u
          for base in 'https://www.pelegobox.com.br' 'https://pelegobox.com.br'; do
            for path in '/_functions/diagnosticoTokenizacaoValidaPay' '/_functions/checkAbandon'; do
              echo "=== $base$path ==="
              body="$(mktemp)"
              code="$(curl --silent --show-error --location --output "$body" --write-out '%{http_code}' "$base$path" || true)"
              echo "HTTP $code"
              head -c 1200 "$body" || true
              echo
              rm -f "$body"
            done
          done
